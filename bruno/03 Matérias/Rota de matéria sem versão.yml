info:
  name: Rota de matéria sem versão
  type: http
  seq: 40

http:
  method: GET
  url: "{{baseUrl}}/subjects"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Não expõe matérias sem versão", function() {
          expect(res.status).to.equal(404);
          expect(res.body.error.code).to.equal("NOT_FOUND");
          expect(res.body.error.hint).to.include("/v1");
        });

settings:
  encodeUrl: true
