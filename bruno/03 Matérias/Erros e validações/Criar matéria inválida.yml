info:
  name: Criar matéria inválida
  type: http
  seq: 9
  tags:
    - atividade-04

http:
  method: POST
  url: "{{baseUrl}}/subjects"
  body:
    type: json
    data: |-
      {}

runtime:
  scripts:
    - type: tests
      code: |-
        test("Rejeita matéria sem campos obrigatórios", function() {
          expect(res.status).to.equal(400);
          expect(res.body.success).to.equal(false);
          expect(res.body.error.code).to.equal("VALIDATION_ERROR");
          expect(res.body.error.details).to.be.an("array");
          expect(res.body.timestamp).to.be.a("string");
          expect(res.body.path).to.equal("/subjects");
        });

settings:
  encodeUrl: true
