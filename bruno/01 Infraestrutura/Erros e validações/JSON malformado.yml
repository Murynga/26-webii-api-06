info:
  name: JSON malformado
  type: http
  seq: 101
  tags:
    - pratica-06

http:
  method: POST
  url: "{{baseUrl}}/users"
  body:
    type: json
    data: |-
      {"segredo":

runtime:
  scripts:
    - type: before-request
      code: |-
        // Nenhuma fixture: o parser rejeita antes do controller.
    - type: tests
      code: |-
        test("Preserva status e envelope para JSON malformado", function() {
          expect(res.status).to.equal(400);
          expect(res.body.success).to.equal(false);
          expect(res.body.error.code).to.equal("VALIDATION_ERROR");
          expect(res.body.path).to.equal("/users");
          expect(res.body.timestamp).to.be.a("string");
        });

settings:
  encodeUrl: true
