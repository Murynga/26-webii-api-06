info:
  name: Health check V1
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/v1/health"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Confirma que a V1 está disponível", function() {
          expect(res.status).to.equal(200);
          expect(res.body.version).to.equal("v1");
          expect(res.body.status).to.equal("OK");
        });

settings:
  encodeUrl: true
