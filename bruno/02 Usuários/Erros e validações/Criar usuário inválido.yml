info:
  name: Criar usuário inválido
  type: http
  seq: 5
  tags:
    - aula-04

http:
  method: POST
  url: "{{baseUrl}}/users"
  body:
    type: json
    data: |-
      {}

runtime:
  scripts:
    - type: tests
      code: |-
        test("Rejeita usuário sem campos obrigatórios", function() {
          expect(res.status).to.equal(400);
          expect(res.body.success).to.equal(false);
          expect(res.body.error.code).to.equal("VALIDATION_ERROR");
          expect(res.body.error.details).to.be.an("array");
          expect(res.body.timestamp).to.be.a("string");
          expect(res.body.path).to.equal("/users");
        });

settings:
  encodeUrl: true
